# 🚀 Simple Start Guide

## ⚡ Just Run This

```
Double-click: start.bat
```

**That's it!** The bot will start with visible browser (non-headless).

---

## 🎯 What Changed

- ✅ **Simplified Python check** - Less strict, more forgiving
- ✅ **Default: Visible browser** (`HEADLESS=false`)
- ✅ **Automatic setup** - Creates venv, installs deps automatically
- ✅ **Works like before** - Simple and reliable

---

## 📋 Options

### Visible Browser (Default)
```
start.bat
```
Browser will be visible - you can see what's happening.

### Hidden Browser
```
start_headless.bat
```
Browser runs in background (after you're logged in).

---

## 🔧 If Python Error Appears

1. **Run this:**
   ```
   check_python.bat
   ```
   It will try to find and fix Python automatically.

2. **Or try:**
   ```
   python --version
   ```
   If this works, Python is fine - just run `start.bat` again.

---

## ✅ That's All!

Just double-click `start.bat` and go! 🎉



